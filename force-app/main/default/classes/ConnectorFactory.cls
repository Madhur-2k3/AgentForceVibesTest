public with sharing class ConnectorFactory {
    /**
     * Factory method to create a connector by type.
     * Consider replacing 'type' with an enum and return a concrete interface instead of Object.
     */
    public static Object createConnector(String type, Map<String, Object> config) {
        // TODO: implement switch on type and instantiate appropriate connector
        return null;
    }
}
